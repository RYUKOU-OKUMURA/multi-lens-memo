name = "multi-lens-memo"
compatibility_date = "2024-12-01"
pages_build_output_dir = "dist"

# ローカル開発: wrangler pages dev dist --port 8788
# デプロイ:     wrangler pages deploy dist

# 環境変数（シークレットは wrangler secret put ZAI_API_KEY で設定）
# [vars]
# ZAI_API_BASE_URL = "https://open.bigmodel.cn/api/paas/v4"
